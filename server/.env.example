# MySQL 데이터베이스 연결 설정
# 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 변경하세요.
# cp server/.env.example server/.env

# 데이터베이스 호스트 (로컬: localhost, 원격: Tailscale IP)
DB_HOST=localhost

# 데이터베이스 포트
DB_PORT=3306

# 데이터베이스 사용자명
DB_USER=root

# 데이터베이스 비밀번호 (실제 비밀번호로 변경하세요!)
DB_PASSWORD=your_password

# 데이터베이스 이름
DB_NAME=pcb_inspection
