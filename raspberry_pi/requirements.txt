# PCB 검사 라즈베리파이 클라이언트 의존성
# Python 3.9+ 권장

# 카메라 처리
opencv-python==4.10.0.84
numpy>=1.24.0

# HTTP 통신
requests>=2.31.0

# 환경 변수 관리
python-dotenv>=1.0.0

# GPIO 제어 (라즈베리파이 전용)
# 주의: 라즈베리파이에서만 설치 가능
# 다른 환경에서는 주석 처리하거나 제외
RPi.GPIO>=0.7.1

# 시스템 도구 (v4l2-ctl 파이썬 바인딩, 선택사항)
# v4l2-python3>=0.3.2
