# Flask 서버 환경 변수 템플릿
# 이 파일을 .env로 복사하여 실제 값을 입력하세요
# cp .env.example .env

# ===== 서버 설정 =====
FLASK_ENV=development
# production / development

SERVER_HOST=0.0.0.0
# 로컬: 0.0.0.0
# 원격: 100.x.x.x (Tailscale IP)

SERVER_PORT=5000

# ===== GPU 설정 =====
GPU_DEVICE=cuda:0
# cuda:0 (GPU 사용) / cpu (CPU 사용)

USE_FP16=true
# true (FP16 사용, VRAM 절약) / false (FP32 사용)

# ===== 모델 경로 =====
YOLO_MODEL_PATH=models/yolo/final/yolo_best.pt
ANOMALY_MODEL_PATH=models/anomaly/padim/model.pth

# ===== 추론 설정 =====
CONFIDENCE_THRESHOLD=0.25
IOU_THRESHOLD=0.45
MAX_BATCH_SIZE=2
# 라즈베리파이 2대 동시 처리

# ===== 데이터베이스 설정 =====
DB_HOST=localhost
# 로컬: localhost
# 원격: 100.x.x.x (Tailscale IP)

DB_PORT=3306
DB_NAME=pcb_inspection
DB_USER=root
DB_PASSWORD=your_password_here
# 실제 비밀번호로 변경 필수!

# ===== 로깅 설정 =====
LOG_LEVEL=INFO
# DEBUG / INFO / WARNING / ERROR

LOG_FILE=logs/server.log
LOG_MAX_BYTES=10485760
# 10MB

LOG_BACKUP_COUNT=5

# ===== CORS 설정 =====
CORS_ORIGINS=*
# 프로덕션에서는 실제 도메인으로 변경
# 예: http://192.168.0.30:3000,http://100.x.x.x:3000
