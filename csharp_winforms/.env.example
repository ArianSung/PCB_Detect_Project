# C# WinForms 모니터링 앱 환경 변수 템플릿
# 이 파일을 .env로 복사하여 실제 값을 입력하세요
# (C#에서는 appsettings.json 사용 권장, 이 파일은 참고용)

# ===== Flask 서버 연결 =====
API_BASE_URL=http://192.168.0.10:5000
# 로컬: http://192.168.0.10:5000
# 원격: http://100.x.x.x:5000 (Tailscale IP)

API_TIMEOUT=10
# HTTP 요청 타임아웃 (초)

# ===== 데이터베이스 연결 =====
DB_HOST=localhost
# 로컬: localhost
# 원격: 100.x.x.x (Tailscale IP)

DB_PORT=3306
DB_NAME=pcb_inspection
DB_USER=root
DB_PASSWORD=your_password_here
# 실제 비밀번호로 변경 필수!

DB_CONNECTION_STRING=Server={DB_HOST};Port={DB_PORT};Database={DB_NAME};Uid={DB_USER};Pwd={DB_PASSWORD};

# ===== 실시간 업데이트 설정 =====
REFRESH_INTERVAL=5000
# 데이터 갱신 주기 (밀리초, 기본 5초)

MAX_DISPLAY_RECORDS=100
# 최대 표시 레코드 수

# ===== 로깅 설정 =====
LOG_LEVEL=Information
# Debug / Information / Warning / Error

LOG_FILE=logs/winforms_app.log

# ===== Excel 내보내기 설정 =====
EXPORT_DEFAULT_PATH=C:\\PCB_Reports
# Excel 파일 기본 저장 경로

EXPORT_FILE_FORMAT=PCB_Report_{0:yyyyMMdd_HHmmss}.xlsx
# 파일명 형식

# ===== 권한 시스템 설정 =====
DEFAULT_USER_ROLE=Viewer
# Admin / Operator / Viewer

SESSION_TIMEOUT=3600
# 세션 타임아웃 (초, 기본 1시간)

# ===== 차트 설정 =====
CHART_MAX_DATA_POINTS=50
# 실시간 차트 최대 데이터 포인트

CHART_UPDATE_INTERVAL=2000
# 차트 갱신 주기 (밀리초, 기본 2초)
